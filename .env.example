# HomeAI server environment variables
# Copy to .env and adjust as needed

# HTTP API port
PORT=3000

# MQTT broker port
MQTT_PORT=1883

# SQLite database path (relative to packages/server or absolute)
DB_PATH=./data/smarthome.db
